<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <!-- TODO (helenparsons): We populate the menu with example action here.  Change this to be
         TODO (helenparsons)  add stock instead.
         TODO (helenparsons)  We show this menu item in MainActivity regardless of which child
         TODO (helenparsons)  fragment is being displayed.  We may need to update this to be
         TODO (helenparsons)  fragment specific if we add fragments for goals (where an add stock
         TODO (helenparsons)  action makes no sense).
      -->
    <item android:id="@+id/action_example" android:title="@string/action_example"
        app:showAsAction="withText|ifRoom" />
</menu>
